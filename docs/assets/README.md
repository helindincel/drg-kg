# Demo assets

Place launch screenshots and optional GIFs here before the public announcement.

Suggested captures (see [`community_launch.md`](community_launch.md)):

- `api-ui-graph.png` — FastAPI UI with loaded graph and entity inspector
- `api-ui-query.png` — deterministic query panel result
- `demo-query-layer.gif` — optional recording of `examples/query_layer_example.py`

These files are documentation-only and are not shipped in the PyPI wheel.
